import { Pending } from "../Pending";

export default function QuotesPage() {
  return (
    <Pending eyebrow="Awaiting design" title="Quote status">
      The deck names this section in the navigation and describes it in prose —
      a quote history table carrying the status of each quotation — but no
      screen has been drawn for it. Listed in `clients.md` §1.1.
    </Pending>
  );
}
