import { Pending } from "../Pending";

export default function SearchPage() {
  return (
    <Pending eyebrow="In progress" title="Search results">
      Results for a part number or description search. The screens exist in the
      deck (slides 17–27); this route is next in the rebuild.
    </Pending>
  );
}
