/** Fail closed until authenticated same-origin routing and private drawing delivery are wired. */
export default function MappingPage() {
  return <main><h1>Mapping editor unavailable</h1><p>Authenticated catalogue access and private drawing delivery are not configured for this frontend. The mapping editor will be available after those services are connected and verified.</p></main>;
}
